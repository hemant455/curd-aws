spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/company_db
    username: root
    password: Hemant@9955

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 9090